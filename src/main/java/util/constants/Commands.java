package util.constants;

/**
 * enum contains commands constants
 */
public enum Commands {
    ADD_BOOKING, APPLICATIONS, BOOKINGS,
    CLIENT_APPLICATIONS, SET_CLIENT,
    FORM_CLIENT_INF, FORM_CLIENT_ROOM,
    MAIN_PAGE, ROOMS, ERROR, REQUEST_STEP1
}
